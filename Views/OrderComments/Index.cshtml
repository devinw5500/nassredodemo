@model IEnumerable<TaxSystemNASS.Models.OrderComment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Comment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Urgent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Comment)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Urgent)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Order.OrderGuid)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.OrderCommentId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.OrderCommentId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.OrderCommentId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>