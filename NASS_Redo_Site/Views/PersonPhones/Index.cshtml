@model IEnumerable<NASS_Redo_Site.Models.PersonPhone>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Phone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Primary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PersonContact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Primary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonContact.PersonContactId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PersonPhoneId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PersonPhoneId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PersonPhoneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
