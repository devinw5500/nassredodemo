@model NASS_Redo_Site.Models.PersonContact

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PersonContact</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PersonContactId" />
            <div class="form-group">
                <label asp-for="PersonInfoId" class="control-label"></label>
                <select asp-for="PersonInfoId" class="form-control" asp-items="ViewBag.PersonInfoId"></select>
                <span asp-validation-for="PersonInfoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
